// en java script se usan los objetos para condensar variables y evitar un desorden en el código, la sintaxis para este es las llaves {}

const producto = {
     nombre: "Monitor de 20 pulgadas",
     precio: 300,
     disponible: true,
}

console.log(producto);

// se debe tener en cuenta que las variables que van a air contenidas dentro del objeto deben ir con (:) y no con (=), tambien deben ir separadas 
//con (,) y no con (;)